# Local dev: point the frontend at the local backend (npm run start:dev on :3001).
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Domain shown to users for the generated short link (no protocol).
NEXT_PUBLIC_SHORT_DOMAIN=localhost:3001
